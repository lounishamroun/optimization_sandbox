[project]
name = "optimization-sandbox"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "depyf>=0.20.0",
    "numpy>=2.4.3",
    "tabulate>=0.10.0",
    "torch>=2.10.0",
    "triton>=3.6.0",
]

[tool.setuptools.packages.find]
where = ["."]
